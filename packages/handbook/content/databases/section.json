{
  "slug": "databases",
  "title": "Databases",
  "order": 12,
  "summary": "What the database does with the query you sent it: which index it picks, why it sometimes picks none, and the access patterns that turn one page load into hundreds of round trips. Then the decision underneath all of that, which is what shape the data was stored in and what that shape charges you. Every page names the engine it means, because the workouts run Postgres and the SQL problems run SQLite."
}
